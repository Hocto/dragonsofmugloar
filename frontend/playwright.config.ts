import { defineConfig, devices } from '@playwright/test'

/**
 * One browser, one flow. The backend is stubbed inside the test with page.route, so this run needs
 * no Java, no Docker and no Mugloar - which is the only way a game that ends in three turns is
 * repeatable. It covers the frontend end to end; the backend has its own suite.
 */
export default defineConfig({
  testDir: './e2e',
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 1 : 0,
  workers: 1,
  reporter: process.env.CI ? 'github' : 'list',
  use: {
    baseURL: 'http://localhost:4173',
    trace: 'on-first-retry',
  },
  projects: [{ name: 'chromium', use: { ...devices['Desktop Chrome'] } }],
  webServer: {
    command: 'npm run build && npx vite preview --port 4173 --strictPort',
    url: 'http://localhost:4173',
    reuseExistingServer: !process.env.CI,
    timeout: 120_000,
  },
})
